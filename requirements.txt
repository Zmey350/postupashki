# Core database, collector, dashboard and sales analysis: Python standard library only.
# Optional machine learning: python -m pip install -r ml/requirements.txt
# Optional experiment calculator: python -m pip install -r analysis/requirements.txt
